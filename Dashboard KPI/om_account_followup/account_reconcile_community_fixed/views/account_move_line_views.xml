<odoo>
    <record id="view_move_line_tree_reconcile" model="ir.ui.view">
        <field name="name">account.move.line.reconcile.view</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='debit']" position="before">
                <button name="action_manual_reconcile"
                        type="object"
                        string="Reconcile"
                        class="oe_highlight"/>
            </xpath>
        </field>
    </record>

    <record id="action_manual_reconcile" model="ir.actions.act_window">
        <field name="name">Manual Reconciliation</field>
        <field name="res_model">account.move.line</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('reconciled', '=', False)]</field>
    </record>

    <menuitem id="menu_account_reconcile"
              name="Reconciliation"
              parent="account.menu_finance_entries"
              action="action_manual_reconcile"
              sequence="90"/>
</odoo>
